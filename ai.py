import __future__
import skynet
import timetravel
import sys

major, minor, micro, releaselevel, serial = sys.version_info
print("Skynet AI is not yet python %s.%s compatible " % (major, minor))
sys.exit(1)
